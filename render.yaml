databases:
  - name: posterose
    databaseName: posterose
    user: posterose_user
    plan: free

services:
  - type: web
    name: posterose
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "./bin/rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "./bin/rails server"
    envVars:
      - key: postgres://posterose_user:Wm8Y1cbdSEgeDz5QPXt7yW7xdUyeX7nu@dpg-cm25cjmn7f5s73esc66g-a.oregon-postgres.render.com/posterose
        fromDatabase:
          name: posterose
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default
